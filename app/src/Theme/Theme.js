// MUI
import { createTheme } from "@mui/material/styles";

let Theme = createTheme({
  palette: {
    primary: {
      main: "#315AEB",
    },
    text: {
      secondary: "#9594A2",
    },
  },
});

export default Theme;
