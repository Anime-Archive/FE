// MUI
import { createTheme } from "@mui/material/styles";

let Theme = createTheme({
  palette: {
    primary: {
      main: "#315AEB",
    },
    secondary: {
      main: "#C4C4C4",
    },
    text: {
      secondary: "#9594A2",
    },
  },
});

export default Theme;
